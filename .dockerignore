# Keep the build context small — the build context is the repo root, but images
# only need source + manifests, never these.
.git
**/.venv
**/node_modules
**/dist
**/.astro
**/__pycache__
**/*.py[cod]
**/.mypy_cache
**/.pytest_cache
**/*.egg-info
*.env
docs
