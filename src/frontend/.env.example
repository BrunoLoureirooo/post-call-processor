# Base URL of the FastAPI backend, inlined into client code via import.meta.env.
# In docker-dev this is set by docker-compose.yml; on the host loop, copy this file
# to .env or rely on the built-in fallback (http://localhost:8000) in UploadForm.tsx.
PUBLIC_API_BASE_URL=http://localhost:8000
